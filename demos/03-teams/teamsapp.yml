# Teams Toolkit Project Configuration
# Visit https://aka.ms/teamsfx-v5.0-guide for details

version: 1.0.0

projectId: databricks-genie-mcp

environmentFolderPath: ./env

# Defines what the `provision` lifecycle step does with Teams Toolkit.
provision:
  - uses: teamsApp/create
    with:
      name: Genie Bot-${{TEAMSFX_ENV}}
    writeToEnvironmentFile:
      teamsAppId: TEAMS_APP_ID

  - uses: botAadApp/create
    with:
      name: Genie Bot-${{TEAMSFX_ENV}}
    writeToEnvironmentFile:
      botId: BOT_ID
      botPassword: SECRET_BOT_PASSWORD

  - uses: teamsApp/validateManifest
    with:
      manifestPath: ./appPackage/manifest.json

  - uses: teamsApp/zipAppPackage
    with:
      manifestPath: ./appPackage/manifest.json
      outputZipPath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip
      outputJsonPath: ./appPackage/build/manifest.${{TEAMSFX_ENV}}.json

  - uses: teamsApp/update
    with:
      appPackagePath: ./appPackage/build/appPackage.${{TEAMSFX_ENV}}.zip

# Defines what the `deploy` lifecycle step does with Teams Toolkit.
deploy:
  - uses: prerequisite/install
    with:
      devCert:
        trust: true

  - uses: file/createOrUpdateEnvironmentFile
    with:
      target: ./.env
      envs:
        BOT_ID: ${{BOT_ID}}
        BOT_PASSWORD: ${{SECRET_BOT_PASSWORD}}
        BOT_ENDPOINT: ${{BOT_ENDPOINT}}
        BOT_DOMAIN: ${{BOT_DOMAIN}}
