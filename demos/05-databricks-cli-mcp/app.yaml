# Databricks CLI MCP Slack Bot
command: ["python", "cli_slack_bot.py"]

resources:
  cpu: "1"
  memory: "1Gi"

env:
  - name: SLACK_BOT_TOKEN
    valueFrom:
      secretKeyRef:
        key: slack-bot-token

  - name: SLACK_APP_TOKEN
    valueFrom:
      secretKeyRef:
        key: slack-app-token
